# Core dependencies (required for both CSV and LLM versions)
pandas>=2.0.0
openpyxl>=3.1.0
streamlit>=1.28.0

# LLM version dependencies (for PDF processing tab)
openai>=1.0.0
anthropic>=0.18.0
PyMuPDF>=1.23.0
